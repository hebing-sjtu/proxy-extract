"""Reader for the source corpus.

`abot` ships colour and a COLMAP sparse model and nothing else — no depth, no
semantics — which is the whole reason the prediction pipeline exists.
"""

from __future__ import annotations

__all__ = ["abot"]
